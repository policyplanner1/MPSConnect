<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to local dev API (LAN IP / emulator) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
